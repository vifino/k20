usage: k20 [options] [port]
-n --name=k20 char* JACK client name
-d --dump bool Dump values for machine consumption (e.g. gnuplot)
-r --rate=30 float Refresh rate
-v --verbose bool Enable verbose output (wide terminal needed)
-C --nocolor bool Disable color output.
-B --nobold bool Don't bold the meter.
-e --ansionly bool Use escapes only instead of ncurses.
